---
paths:
    - "**/*.py"
    - "**/*.pyi"
---

# Python Hooks

## Project Hooks

The repository hook in `.claude/settings.json` owns targeted formatting, linting, and file hygiene after edits. Pre-commit owns staged gates, and CI owns repository-wide gates.
