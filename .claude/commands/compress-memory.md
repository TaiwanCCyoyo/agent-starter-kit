---
description: Compress .memories/ when bounded memory files are too large, duplicated, stale, or poorly routed.
---

# Compress Memory

Use the `compress-memory` skill to audit and compress bounded memory without losing durable lower-frequency knowledge.
